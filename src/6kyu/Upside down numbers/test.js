import { solve } from '.'

test('Upside down numbers', () => {
  expect(solve(0, 10)).toBe(3)
})

test('Upside down numbers', () => {
  expect(solve(6, 69)).toBe(2)
})

test('Upside down numbers', () => {
  expect(solve(10, 9966)).toBe(35)
})
